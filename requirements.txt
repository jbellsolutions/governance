# ── Integrations sidecar — specialist agent layer (Agency Swarm / OpenSwarm) ──
# Paperclip is the orchestration plane; this repo only needs the sidecar stack.
agency-swarm>=0.5.1

# ── API server ───────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9        # FastAPI form parsing

# ── LLM clients ─────────────────────────────────────────────────────────────
openai>=1.0.0

# ── Config ───────────────────────────────────────────────────────────────────
pydantic>=2.0.0
python-dotenv>=1.0.0

# ── Optional: Composio integrations (10k+ apps) ──────────────────────────────
# composio-openai>=0.5.0      # install when COMPOSIO_API_KEY is set
#   pip install composio-openai && composio add slack gmail hubspot

# ── Optional: web search for research specialist ──────────────────────────────
# google-search-results>=2.4.2  # needed for SerperDevTool
